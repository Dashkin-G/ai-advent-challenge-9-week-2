# Обязательная переменная — ключ API.
DASHSCOPE_API_KEY=

# Модель агента по умолчанию (переопределяется в интерфейсе).
# DEFAULT_MODEL=qwen3.8-max

# Параметры HOST/PORT указываем при развёртывании на сервере.
# HOST=0.0.0.0
# PORT=8000
